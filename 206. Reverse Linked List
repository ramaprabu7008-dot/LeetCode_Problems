  class Solution {
    public ListNode reverseList(ListNode head) {
        ListNode curr=head,p=null,next=null;
         while(curr!=null)
         {
            next=curr.next;
            curr.next=p;
            p=curr;
            curr=next;
         }
         return p;   
    }
}
